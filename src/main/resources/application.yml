
spring:
  profiles:
    active: dev
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME} 
    password: ${SMTP_PASSWORD} 
    properties:

weather:
  api:
    key: ${WEATHER_API_KEY} 
